<view wx:if="{{value == 'label_1'}}">
    <view class="block">
        <t-navbar class="custom-navbar" title="首页" t-class-title="nav-title" />
    </view>
</view>
<view wx:if="{{value == 'label_2'}}">
    <view class="block">
        <t-navbar class="custom-navbar" title="操作中" t-class-title="nav-title" />
    </view>
</view>
<view wx:if="{{value == 'label_3'}}">
    <view class="block">
        <t-navbar class="custom-navbar" title="我的" t-class-title="nav-title" />
    </view>
    <view class="minetitle">
        <view style="width: 80%;margin-left: 10%;padding-top: 50rpx;display: inline-block;">
            <t-avatar size="large" icon="user" wx:if="{{image==null||image==''}}" bindtap="goMine" />
            <t-avatar size="large" image="{{image}}" wx:else bindtap="goMine" />
            <view style="display: inline-block;margin-top: 10px;margin-left: 10px;">
                <view style="margin-bottom: 4px;font-weight: 600;font-size: 18px;">{{myUserName}}</view>
                <view>{{myTelephone}}</view>
            </view>
        </view>
    </view>
    <view class="applypage">
        <t-grid class="applygird" column="{{4}}">
            <t-grid-item text="历史订单" icon="file" bindclick="onClick" />
            <t-grid-item text="账单" icon="money-circle" bindclick="onClick" />
            <t-grid-item text="公司信息" icon="root-list" bindclick="onClick" />
            <t-grid-item text="地址" icon="location" bindclick="onClick" />
            <t-grid-item text="装箱拍照" icon="photo" bindclick="onClick" />
            <t-grid-item text="进场出场" icon="desktop" bindclick="gocontainerinout" />
            <t-grid-item text="联系平台" icon="call" bindclick="callClick" />
        </t-grid>
    </view>
    <view class="button-example">
        <t-button theme="primary" size="large" block icon='logout' bindtap="logout">退出登录</t-button>
    </view>
    <view class="footer-example">
        <t-footer text="{{text}}" links="{{links}}" />
    </view>
</view>

<t-tab-bar value="{{value}}" bindchange="onChange" theme="tag" split="{{false}}">
    <t-tab-bar-item wx:for="{{list}}" wx:key="index" value="{{item.value}}" icon="{{item.icon}}">
        {{item.label}}
    </t-tab-bar-item>
</t-tab-bar>

<t-dialog visible="{{showTextAndTitle}}" title="未登录" content="必须先登录才可以查看订单数据" confirm-btn="登录/注册" bind:confirm="goLogin" />
<t-dialog visible="{{showlogout}}" title="退出登录" content="确认退出？" confirm-btn="{{ logoutconfirmBtn }}" cancel-btn="取消" bind:confirm="logoutConfirm" bind:cancel="closelogout" />
<t-dialog visible="{{showCall}}" title="联系电话" content="{{phoneNumber}}" confirm-btn="{{ callconfirmBtn }}" cancel-btn="取消" bind:confirm="callConfirm" bind:cancel="closecall" />